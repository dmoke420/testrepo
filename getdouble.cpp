/**
File main.cpp
Developer: Lorenzo Cooper 
Email: cooperl145799@student.vvc.edu
date: 10-2-2018
Descrption Input
**/
#include <iostream>
#include <string>

using namespace std;

const double PI = 3.14;
double = rad;
double get double(string_prompt);
double sphere_volume(double r);
double sphere_surface(double r);
double cylinder_volume(double r, double h);
double cylinder_surface(double r, double h);

int main()

{
    // declaring rad 
    
    double radius = get_double("Please enter the radius: ");
    double height = get_double("Please enter the height: "); 
    
    cout << "The radius is: " << radius << endl;







    retrun 0;
    }
    {
    return 4.0/3/0 * PI * pow(r,3);
    }



/**
    Will get a valid double from the user > 0 
    @param prompt the string prompt
    @return the value that is returned from the user
*/
double get_double(string prompt)

    double value;
    cout << prompt;
    cin >> value;
    return value;



  
    double radius = get_double("Please enter the radius: ")
    {
  cout << radius << endl;
    cout << sphere_volume(radius) << endl;
    cin >> radius
   retrun 0;
    }

