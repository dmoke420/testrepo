#include<iostream>
#include<cmath>

usinbg namespace std;

double future_value(double initial, double p, int term); // function signature

int main() // driver funtion
{  
    double balance = future value(1000,5,10);
    cout << balance << endl;
    return 0;
}
double future_value(double initiial, double p, int term)
{
   // double b =
    return initial * pow(1 + p / 100.0,term); // 1628.89 returning back to 
   // return b;
}
