#include <iostream>
#include <cstdlib>
#include <string>

using namespace std;
int main ()
{
    int testval = 0;

    if (( 10 <= testval) && (testval <= 100))
    {
        cout << "The twat value is between 10 and 100";
        } else 
        {
            cout << "The test value was not between 10 and 100";
            }
            return 0;
     }
